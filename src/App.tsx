import { motion } from "framer-motion";
import BouncingLoader from "./components/BouncingLoader";
const App = () => {
  return (
    <div>
      <BouncingLoader />
    </div>
  );
};

export default App;
